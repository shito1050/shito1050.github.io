# AGENTS.md

このリポジトリは，高校数学教材サイト「すうがくがかり」のGitHub Pages用リポジトリである。
AIエージェントがこのリポジトリを編集する場合は，必ずこのファイルの方針に従うこと。

## 1. サイトの目的

* このサイトは，高校生向けの数学教材サイトである。
* 主なコンテンツは，定義・用語・公式集，授業，問題演習，今日の1問，入場問題，ブログである。
* サイトはGitHub Pagesで公開する静的サイトであり，HTML，CSS，JavaScriptで構成する。
* 長期的に問題数・用語数・授業数が増える前提で，保守しやすい構成を優先する。

## 2. 基本方針

* 既存のファイル構成，デザイン，文言，データ形式を尊重する。
* 勝手に大規模な設計変更をしない。
* 1つのPull Requestでは，原則として1つの目的だけを扱う。
* 修正前に関連ファイルを確認し，既存の書き方に合わせる。
* 不明点がある場合は，推測で進めず質問する。
* ユーザーに無断で説明文・案内文・補足文を増やさない。
* 一時的に動くだけの修正ではなく，将来の拡張に耐える修正を優先する。
* ただし，指示がない限り，既存仕様を壊すような大規模変更はしない。

## 3. 重要な禁止事項

* `dictionary` を `definition` や `definitions` に変更しない。
* `definitions.html` というページを作らない。
* 旧 `script.js` を前提にした実装をしない。
* 問題ボックス内に問題タイトルを重複表示しない。
* サイト用MathJaxで独自TeXマクロを使わない。
* `\tmaru` などの独自マクロを使わない。
* 丸囲み数字を `(①)` のように括弧つきで表示しない。
* サイト内で「見る」と書かない。原則として「みる」にする。
* 求値問題の解答末尾に「（終）」を入れない。
* 問題文・解答文を，指示なく言い換えない。
* 画像から追加する問題では，読み取れない部分を推測で補わない。

## 4. 表記ルール

* 数学に関する文章では，句読点は原則として `，` と `．` を使う。
* 数学以外の一般文では，必要に応じて `、` と `。` を使ってよい。
* サイト内の文言では「見る」ではなく「みる」を使う。
* 高校数学サイト内の不等号は，原則として `\leqq`，`\geqq` を使う。
* 丸囲み数字は `①`，`②`，`③` のようなUnicode文字を使う。
* 丸囲み数字を括弧で囲んで `(①)` のようにしない。
* 平行は `//` と表記する。
* 点名は `\mathrm{A}` のように立体で表す。
* 線分は `\mathrm{AB}` のように表す。

## 5. フォルダ・ファイル構成の方針

* 定義・用語・公式集は `dictionary/` に置く。
* 問題演習は `practice/` に置く。
* 問題データは `data/practice/` 以下に分割して置く。
* 入場問題データは，問題演習データとは別に管理する。
* 画像は `assets/images/practice/` 以下に置く。
* 数学Bのフォルダ名は `mathb`，数学Cのフォルダ名は `mathc` のように小文字にする。
* ただし，JavaScript上の単元IDは既存仕様を尊重し，`mathB_sequences`，`mathC_vectors` のような大文字を含むIDを勝手に変更しない。
* 定義・用語・公式集のフォルダ名は必ず `dictionary` とする。

## 6. 共通読み込みの方針

* 共通処理は，既存の `common-loader.js` と分割されたJavaScriptファイルの構成を尊重する。
* すべてを1つのファイルにまとめるような変更はしない。
* キャッシュ対策のバージョン文字列がある場合は，必要に応じて更新する。
* MathJax設定を変更する場合は，既存ページへの影響を確認する。

## 7. 問題演習データの基本形式

問題データは，既存形式に合わせて次の形を基本とする。

```js
window.practiceProblems = window.practiceProblems || [];

window.practiceProblems.push(
  {
    id: "...",
    order: "...",
    title: "...",
    listTitle: "...",
    subject: "...",
    unit: "...",
    topic: "...",
    unitIds: ["..."],
    difficulty: 1,
    questionHtml: `
      ...
    `,
    answerHtml: `
      <h2>解答</h2>
      ...
    `
  }
);
```

各項目の意味は次の通りである。

* `id` は問題固有のIDである。
* `order` は一覧表示用の並び順である。
* `title` と `listTitle` は，既存の命名規則に合わせる。
* `subject` は「数学I」「数学A」「数学II」「数学B」「数学C」「数学III」などとする。
* `unit` は単元名とする。
* `topic` は問題の細かい分類とする。
* `unitIds` は既習範囲設定と対応するIDにする。
* `difficulty` は 1，2，3 を使う。
* `questionHtml` に問題文を書く。
* `answerHtml` に解答を書く。

## 8. 問題演習ページの表示方針

* 問題ボックス内に，問題タイトルを重複表示しない。
* ページ上部にタイトルがある場合，問題ボックス内は「問題」ラベルのあと，すぐ問題文から始める。
* 求値問題の最終答案は，できるだけ次の形式で表示する。

```html
<p>したがって，答えは</p>

<div class="math-block answer">
  \[
  ...
  \]
</div>
```

* ただし，領域や図示など，単独の数式として書けない答えでは，無理にこの形式にしない。
* `answerHtml` の末尾に `<p class="end-mark">（終）</p>` は入れない。
* 解答部分には，必要に応じて `<h2>解答</h2>` を入れる。

## 9. MathJaxの注意

* サイト用MathJaxでは，安全なTeXだけを使う。
* 独自TeXマクロを使わない。
* `\tmaru` などは使わず，丸囲み数字は `①` のように直接書く。
* インライン数式でも，分数などは必要に応じて `\displaystyle` を使って見やすくする。
* 数式番号として丸囲み数字を出す場合も，`(①)` ではなく `①` の形にする。
* サイト表示で壊れる可能性のあるTeXコマンドは避ける。

## 10. 定義・用語・公式集の方針

* フォルダ名は必ず `dictionary` とする。
* `definition`，`definitions`，`definitions.html` は使わない。
* 用語データでは，短い説明は端的に書く。
* 詳しい説明では，高校数学の範囲で，必要に応じて例や証明を入れてよい。
* 数学的に誤った簡略化をしない。
* 用語の五十音グループでは，「ど」で始まる語は「と」に入れる。
* 英字の用語は「英字」グループに入れる。

## 11. 今日の1問の方針

* 今日の1問は毎朝6時に更新する。
* 6時より前は前日扱いとする。
* 既習範囲設定に基づいて出題対象を決める。
* 既習範囲を変更しても，その日の問題が不必要に変わらないようにする。
* 「解答をみる」で演習済み扱いにする。
* 難易度フィルターがある場合，最後の1つは外せないようにする。

## 12. 入場問題の方針

* 入場問題は，問題演習ページの問題とは別の専用データとして管理する。
* 1時間以上未操作のあとにサイトを訪れたとき，入場問題を表示する。
* 正解した場合は，すぐに「サイトに入る」を有効にする。
* 不正解の場合は，解説を表示しながらカウントダウンし，終了後に「サイトに入る」を有効にする。
* 不正解時の解説表示中に，カウントダウン完了でレイアウトが大きくずれないようにする。
* 入場問題は10秒程度で解ける短い問題を基本とする。

## 13. 画像の扱い

* 問題画像は `assets/images/practice/` 以下に置く。
* 教科・単元ごとにフォルダを分ける。
* 画像ファイル名は英数字とハイフンを基本にする。
* 画像を参照するときは，既存ページの階層に合わせて相対パスを正しく設定する。
* 画像から問題を追加する場合，問題文・解答文は画像に書かれた内容を一言一句尊重する。
* 読み取れない文字がある場合は，推測で補わず質問する。

## 14. Pull Requestの作り方

* 変更前に関連ファイルを確認する。
* 既存の構成と命名規則に合わせる。
* 1つのPull Requestで複数の大きな機能を混ぜない。
* Pull Requestの説明には，変更内容と確認内容を書く。
* 可能なら，確認したページのURLやファイル名を書く。
* 表示確認を行った場合は，どの画面を確認したかを書く。
* 不明点がある場合は，Pull Request内に明記するか，作業前に質問する。

## 15. AIエージェントへの注意

* このサイトでは，管理者が細かい文言や表示形式を重視している。
* そのため，良かれと思って文言を追加したり，言い換えたりしない。
* 既存仕様を変えるときは，必ず理由を明確にする。
* 「より一般的な実装」にするために，サイト固有の仕様を消してはいけない。
* 修正は，現在のサイトの見た目・構成・運用に合わせて行う。
* 大量のファイルに同じ変更が必要な場合は，手作業で個別修正する前に，共通化・部品化・自動化できるか検討する。
